# Additional python dependencies for DRF (drf wrapper is shipped with this repository under drf/)
rpy2==3.5.2
pandas>=1.3.5

# R requirements = drf package
# Install by running the following command in R
# install.packages("drf")
